# assets/

Drop the following files here — the site references them by these exact names:

| File | Used by | Notes |
|------|---------|-------|
| `ilias-blioumis.jpg` | About section portrait | 3:4 ratio (portrait) works best. A styled "Portrait coming soon" placeholder shows until this exists. |
| `Ilias-Blioumis-CV.pdf` | "Download CV" button (About) | The button links here; add the PDF to make it work. |

Optional Tableau dashboard previews live in `../images/` (e.g. `dashboard-1.gif`).
